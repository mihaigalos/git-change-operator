---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: gitchangeoperators.gco.galos.one
spec:
  group: gco.galos.one
  names:
    kind: GitChangeOperator
    listKind: GitChangeOperatorList
    plural: gitchangeoperators
    singular: gitchangeoperator
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: GitChangeOperator is the Schema for the gitchangeoperators API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GitChangeOperatorSpec defines the desired state of GitChangeOperator
            properties:
              additionalValues:
                description: Additional values for Helm chart customization
                type: object
                x-kubernetes-preserve-unknown-fields: true
              crds:
                description: CRDs configuration
                properties:
                  install:
                    description: Install specifies whether to install CRDs
                    type: boolean
                type: object
              image:
                description: Image configuration for the operator
                properties:
                  pullPolicy:
                    description: PullPolicy is the image pull policy
                    type: string
                  repository:
                    description: Repository is the container image repository
                    type: string
                  tag:
                    description: Tag is the container image tag
                    type: string
                type: object
              ingress:
                description: Ingress configuration
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the Ingress
                    type: object
                  enabled:
                    description: Enabled specifies whether Ingress is enabled
                    type: boolean
                  hosts:
                    description: Hosts is the list of hosts for the Ingress
                    items:
                      description: IngressHost defines an Ingress host configuration
                      properties:
                        host:
                          description: Host name
                          type: string
                        paths:
                          description: Paths for this host
                          items:
                            description: IngressPath defines an Ingress path configuration
                            properties:
                              backend:
                                description: Backend defines the backend service
                                properties:
                                  service:
                                    description: Service backend
                                    properties:
                                      name:
                                        description: Name of the service
                                        type: string
                                      port:
                                        description: Port of the service
                                        properties:
                                          number:
                                            description: Number is the port number
                                            format: int32
                                            type: integer
                                        type: object
                                    type: object
                                type: object
                              path:
                                description: Path to match
                                type: string
                              pathType:
                                description: PathType (Prefix, Exact, ImplementationSpecific)
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  ingressClassName:
                    description: IngressClassName is the IngressClass to use
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the Ingress
                    type: object
                  name:
                    description: Name of the Ingress resource
                    type: string
                  tls:
                    description: TLS configuration
                    items:
                      description: IngressTLS defines TLS configuration for Ingress
                      properties:
                        hosts:
                          description: Hosts covered by this TLS configuration
                          items:
                            type: string
                          type: array
                        secretName:
                          description: SecretName containing the TLS certificate
                          type: string
                      type: object
                    type: array
                type: object
              metrics:
                description: Metrics configuration
                properties:
                  enabled:
                    description: Enabled specifies whether metrics are enabled
                    type: boolean
                  service:
                    description: Service configuration for metrics
                    properties:
                      port:
                        description: Port is the service port
                        format: int32
                        type: integer
                      type:
                        description: Type is the Kubernetes service type
                        type: string
                    type: object
                  serviceMonitor:
                    description: ServiceMonitor configuration
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations to add to the ServiceMonitor
                        type: object
                      enabled:
                        description: Enabled specifies whether ServiceMonitor is enabled
                        type: boolean
                      interval:
                        description: Interval is the scrape interval
                        type: string
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels to add to the ServiceMonitor
                        type: object
                      name:
                        description: Name of the ServiceMonitor resource
                        type: string
                      scrapeTimeout:
                        description: ScrapeTimeout is the scrape timeout
                        type: string
                    type: object
                type: object
              operator:
                description: Operator configuration
                properties:
                  leaderElect:
                    description: LeaderElect enables leader election
                    type: boolean
                  metricsAddr:
                    description: MetricsAddr is the address for metrics endpoint
                    type: string
                  probeAddr:
                    description: ProbeAddr is the address for health probe endpoint
                    type: string
                type: object
              rbac:
                description: RBAC configuration
                properties:
                  create:
                    description: Create specifies whether to create RBAC resources
                    type: boolean
                type: object
              replicaCount:
                description: ReplicaCount is the number of replicas for the operator
                  deployment
                format: int32
                type: integer
              serviceAccount:
                description: ServiceAccount configuration
                properties:
                  create:
                    description: Create specifies whether to create a ServiceAccount
                    type: boolean
                  name:
                    description: Name is the name of the ServiceAccount to use
                    type: string
                type: object
            type: object
          status:
            description: GitChangeOperatorStatus defines the observed state of GitChangeOperator
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the operator's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the last generation observed by
                  the controller
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the operator deployment
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
