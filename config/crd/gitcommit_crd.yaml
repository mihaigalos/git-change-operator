apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: gitcommits.git.galos.one
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
spec:
  group: git.galos.one
  names:
    kind: GitCommit
    listKind: GitCommitList
    plural: gitcommits
    singular: gitcommit
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.repository
      name: Repository
      type: string
    - jsonPath: .spec.branch
      name: Branch
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.commitSHA
      name: CommitSHA
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              authSecretKey:
                type: string
              authSecretRef:
                type: string
              branch:
                type: string
              commitMessage:
                type: string
              files:
                items:
                  properties:
                    content:
                      type: string
                    path:
                      type: string
                  required:
                  - content
                  - path
                  type: object
                type: array
              repository:
                type: string
            required:
            - authSecretRef
            - branch
            - commitMessage
            - files
            - repository
            type: object
          status:
            properties:
              commitSHA:
                type: string
              lastSync:
                format: date-time
                type: string
              message:
                type: string
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}