apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: pullrequests.git.galos.one
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
spec:
  group: git.galos.one
  names:
    kind: PullRequest
    listKind: PullRequestList
    plural: pullrequests
    singular: pullrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.repository
      name: Repository
      type: string
    - jsonPath: .spec.title
      name: Title
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.pullRequestNumber
      name: PR Number
      type: integer
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              authSecretKey:
                type: string
              authSecretRef:
                type: string
              baseBranch:
                type: string
              body:
                type: string
              files:
                items:
                  properties:
                    content:
                      type: string
                    path:
                      type: string
                  required:
                  - content
                  - path
                  type: object
                type: array
              headBranch:
                type: string
              repository:
                type: string
              title:
                minLength: 1
                type: string
            required:
            - authSecretRef
            - baseBranch
            - files
            - headBranch
            - repository
            - title
            type: object
          status:
            properties:
              lastSync:
                format: date-time
                type: string
              message:
                type: string
              phase:
                type: string
              pullRequestNumber:
                type: integer
              pullRequestURL:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}